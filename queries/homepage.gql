query Homepage ($locale: String!) {
    Homepage {
        fullname
        baseline
        links
        description

        translations(filter: {languages_code: {code: {_eq: $locale}}}) {
            baseline
            description
            legal
        }

        icon {
            title
            id
        }

        user {
            title
            id
        }

        
    }
}
